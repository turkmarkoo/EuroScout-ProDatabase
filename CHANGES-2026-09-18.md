# EuroScout — scouting sessions build (18/09/2026)

Upload these 7 files to the repo root, replacing the three that already exist.
Nothing in `data/`, `config.js` or `overrides.js` is touched. Hard-reload afterwards.

| File | |
|---|---|
| `index.html` | changed — two new synced keys in `APP_KEYS`, script/style tags at the end |
| `euroscout-live-notes.js` | rewritten — Scouting matchup, compact layout |
| `euroscout-live-notes.css` | rewritten |
| `mt-shortcuts.js` | new — shortcut manager, no dependencies, reusable in the hub |
| `euroscout-shortcuts.js` | new — EuroScout's own shortcuts + Settings entry |
| `euroscout-sessions.js` | new — sessions, review status, history, Team Log, coverage |
| `euroscout-sessions.css` | new |

## Where things are stored
- `euroscout:sessions:v1` — finished sessions, synced with the other app data.
- `euroscout:session:active:v1` — the running session, this device only.
- `euroscout:shortcuts:v1` — shortcut bindings per account (cloud copy, editors only);
  everyone also keeps a local copy under `mt:shortcuts:v1:euroscout:<email>`.
- Each reviewed player: `_workflow.viewings[]` gains `sessionId`, `status`, `competition`;
  `_workflow.noteTimes` holds when each note was written.

## Tunables (top of `euroscout-sessions.js`)
`DWELL_MS` (5 s to count as viewed), `FAM` (familiarity weights), `FAM_BANDS`.

## Default shortcuts (all changeable: Settings → Keyboard shortcuts, or press ?)
↑ ↓ player · ← → other team · L A O D I P category · N new note · Esc leave note ·
/ search notes · F search roster · S similar notes · W watchlist · H history ·
Enter profile · + − stock · X swap · Shift+S start/finish session ·
Alt+↑ Alt+↓ Alt+N work while typing.
Anywhere: ? help · Ctrl+K search · Shift+D/P/W/T/M/L go to a page.
