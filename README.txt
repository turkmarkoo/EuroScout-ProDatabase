EuroScout player database update

Upload index.html to the root of the EuroScout-ProDatabase repository and replace the existing index.html. This update adds progressive infinite scrolling, richer database rows and correct photo/initial fallback behavior. It does not change Scouting Matchup or sync settings.

The local source commit is ef37339.
